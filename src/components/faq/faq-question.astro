---
import Heading from '../heading.astro';

export type Props = {
  title: string;
  content: string;
};

const { title, content } = Astro.props;
---

<details class="text-left bg-white/10 rounded-sm text-white px-8">
  <summary class="py-8 hover:opacity-80 cursor-pointer">
    <Heading class="inline-block pl-2">
      {title}
    </Heading>
  </summary>
  <div set:html={content} class="pb-8" />
</details>

<style lang="scss">
  [data-expanding] {
    overflow: hidden;
    max-height: 0;
    transition: all 0.3s ease-in-out;
  }

  input:checked ~ [data-expanding] {
    max-height: 400px;
    @apply pb-8;
  }
</style>
