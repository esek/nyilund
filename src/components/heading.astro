---
export type Props = {
  class?: string;
  larger?: boolean;
};

const { larger, class: className } = Astro.props;
---

<h2
  class:list={[
    'font-bold',
    { 'text-2xl': !larger, 'text-3xl': larger },
    className,
  ]}
>
  <slot />
</h2>
