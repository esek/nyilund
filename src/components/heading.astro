---
export type Props = {
  class?: string;
  larger?: boolean;
};

const { larger, class: className } = Astro.props;
---

<h2
  class:list={[
    'font-bold',
    { 'text-lg md:text-xl': !larger, 'text-2xl md:text-3xl': larger },
    className,
  ]}
>
  <slot />
</h2>
